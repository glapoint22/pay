import { InputFieldDirective } from './input-field.directive';

describe('InputFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFieldDirective();
    expect(directive).toBeTruthy();
  });
});
